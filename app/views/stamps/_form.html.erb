<!-- <%= form_with(model: stamp, local: true) do |form| %>
  <% if stamp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stamp.errors.count, "error") %> prohibited this attendee from being saved:</h2>

      <ul>
      <% stamp.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sign_in %>
    <%= form.text_field :sign_in %>
  </div>

  <div class="field">
    <%= form.label :sign_out %>
    <%= form.text_field :sign_out %>
  </div>

  <!-- <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div> -->

  <!-- <div class="actions">
    <%= form.submit %>
  </div>
<% end %> --> -->
